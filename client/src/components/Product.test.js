import { render, screen } from '@testing-library/react';
import Product from './Product';

describe("Product should have name", () => {
    it("should render a product card with a name", async () => {
        const user = {}
        expect(user).toMatchObject({})
    })
})